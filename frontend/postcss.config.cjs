// Empty PostCSS config so Next won't try to load PostCSS plugins when they are
// not installed in this environment. For full Tailwind integration install
// Tailwind/PostCSS packages and replace this file with the standard config.
module.exports = {
  plugins: {},
}
